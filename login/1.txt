css:
h1{
    text-align: center;
}

button{
    background-color: #333;
    color: whitesmoke;
    border: none;
    cursor: pointer;
    font-size: 1em;
    padding: 10px;
    border-radius: 5px;
    transition: 0.3s;
}
.submit:hover{
    background-color: #46d123;
    color: black;
}

#register-button {
    display: flex;
    flex-direction: row;
    gap: 10px;
    justify-content: center;
    align-items: center;
    margin: 20px;
    padding: 20px;
    text-align: center;
}

div {
    display: flex;
    align-items: center;
    flex-direction: column;
}

input{
    padding: 10px;
    border-radius: 5px;
    border: 1px solid #333;
}

span{
    color: red;
}

.cancle:hover{
    background-color: #d12323;
    color: black;
}

form{
    display: grid;
    grid-template-columns: repeat(auto-fill, 250px);
    justify-content: center;
}


html:
<!-- <h1>Sign-Up</h1>
    <form class="container" [formGroup]="signupForm" (ngSubmit)="handleRegister()">
        <div class="group1">
            <label>Name</label>
            <input type="text" formControlName="name" placeholder="Enter Name"><br>
        </div>
        <div class="group2">
            <label>Email</label>
            <input type="email" formControlName="email" placeholder="Enter Email">
            <span *ngIf="signupForm.get('email')?.invalid && signupForm.get('email')?.touched">Email is Invalid/Required</span> <br>
        </div>
        <div class="group3">
            <label>Mobile</label>
            <input type="text" formControlName="mobile" placeholder="Enter Mobile"><br>
        </div>
        <div class="group4">
            <label>Password</label>
            <input type="password" formControlName="password" placeholder="Enter Password">
        </div>
    </form>
    <div class="group5" id="register-button">
        <button class="submit" type="submit">Save</button>

        <button class="cancle" type="cancle" (click)="cancle()">Cancel</button>
    </div>  -->


ts:
 import { Component } from '@angular/core';
 import { FormBuilder, FormControl, FormGroup, Validators } from '@angular/forms';
 import { User } from '../models/user';
 import { LoginService } from '../services/login.service';

 @Component({
   selector: 'app-register',
   standalone: false,
   templateUrl: './register.component.html',
   styleUrls: ['./register.component.css'] 
 })
 export class RegisterComponent {
   signupForm: FormGroup = new FormGroup({
     name: new FormControl(null, Validators.required),
     email: new FormControl(null, [Validators.required, Validators.email]),
     mobile: new FormControl(null, Validators.required),
     password: new FormControl(null, [Validators.minLength(6), Validators.maxLength(15), Validators.required])
   });

   u: User = new User();
   constructor(public us: LoginService) {}

   handleRegister() {
     this.us.addUser(this.signupForm.value);
     this.signupForm.reset();
   }

   cancle(){
     this.signupForm.reset();
   }
 }

 mask:
 return aadhar.slice(0, 4) + '****' + aadhar.slice(-4);